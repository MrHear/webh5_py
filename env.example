# =========================================
# OneSpace Backend 环境变量配置模板
# =========================================
# 
# 使用说明:
# 1. 复制此文件为 .env
# 2. 修改所有敏感值为实际的安全密码
# 3. 绝对不要将 .env 文件提交到 Git!
#
# =========================================

# --- 运行模式 ---
DEBUG=false

# --- 数据库配置 ---
DB_HOST=mysql
DB_PORT=3306
DB_USER=onespace
DB_PASSWORD=CHANGE_ME_TO_STRONG_PASSWORD
DB_NAME=onespace

# --- JWT 安全配置 ---
# 使用以下命令生成安全密钥:
# python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=CHANGE_ME_TO_RANDOM_SECRET_KEY
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30

# --- 管理员配置 ---
ADMIN_USERNAME=admin
# 使用 scripts/generate_password_hash.py 生成密码哈希
# python scripts/generate_password_hash.py YourStrongPassword
ADMIN_PASSWORD_HASH=CHANGE_ME_TO_PASSWORD_HASH

# --- CORS 配置 ---
# 多个域名用逗号分隔
CORS_ORIGINS=["https://your-domain.com"]

# --- 速率限制 ---
RATE_LIMIT_PER_MINUTE=60

# --- 文件上传 ---
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE_MB=10

# --- Redis 配置 ---
# 用于存储每日审核 API 调用计数等
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_TO_REDIS_PASSWORD
REDIS_DB=0

# --- 内容审核配置 ---
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
MODERATION_ENABLED=true
MODERATION_DAILY_LIMIT=500
